import { type Locator, type Page } from '@playwright/test';\n\n/**\n * MyTripsPage - Page Object Model for the \"My Trips\" page\n */\nexport class MyTripsPage {\n  readonly page: Page;\n  readonly myTripsButton: Locator;\n  readonly seeAllTripsButton: Locator;\n  readonly tripDetails: Locator;\n  readonly deleteTripButton: Locator;\n  readonly deleteTripPopUp: Locator;\n\n  constructor(page: Page) {\n    this.page = page;\n    this.myTripsButton = page.getByRole('button', { name: 'My trips' });\n    this.seeAllTripsButton = page.getByRole('link', { name: 'See all trips' });\n    this.tripDetails = page.locator('.list > section > .rt-menu > .rt-button');\n    this.deleteTripButton = page.getByRole('button', { name: 'Delete Trip' });\n    this.deleteTripPopUp = page.getByRole('button', { name: 'Delete' });\n  }\n\n  async deleteTrip(): Promise<void> {\n    await this.myTripsButton.click();\n    await this.seeAllTripsButton.isVisible({ timeout: 5000 });\n    await this.seeAllTripsButton.click();\n    await this.tripDetails.click();\n    await this.deleteTripButton.click();\n    await this.deleteTripPopUp.click();\n  }\n}\n