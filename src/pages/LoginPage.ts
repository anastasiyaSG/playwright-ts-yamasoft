import { type Locator, type Page } from '@playwright/test';\n\n/**\n * LoginPage - Page Object Model for the Roadtrippers login flow\n */\nexport class LoginPage {\n  readonly page: Page;\n  readonly loginButton: Locator;\n  readonly loginPopupHeadline: Locator;\n  readonly username: Locator;\n  readonly password: Locator;\n  readonly submitButton: Locator;\n\n  constructor(page: Page) {\n    this.page = page;\n    this.loginButton = page.getByRole('link', { name: 'Log in' });\n    this.loginPopupHeadline = page.getByRole('heading', { name: 'Log in to your account' });\n    this.username = page.locator('input[name=\"login\"]');\n    this.password = page.getByRole('textbox', { name: 'Password' });\n    this.submitButton = page.getByRole('button', { name: 'Log in' });\n  }\n\n  async login(username: string, password: string): Promise<void> {\n    await this.username.fill(username);\n    await this.password.fill(password);\n  }\n}\n